# 第 1 章 什么是大模型推理

## 本章导读

如果你是一个会写代码的工程师，但不太了解大语言模型（LLM）内部到底发生了什么，这章就是为你写的。我们会用最直白的语言解释"推理"这件事，不堆砌公式，先建立直觉。读完本章，你会明白：

- 大模型推理和训练有什么区别
- "生成一个字"背后经历了哪些步骤
- 为什么推理速度是个难题
- ds4 这个项目在整个生态中扮演什么角色

---

## 1.1 先搞清楚：训练 vs 推理

你可以把大模型想象成一个**读完了整个互联网的学生**。

- **训练（Training）**：是这个学生学习的过程。它阅读海量文本，不断调整自己脑子里的"知识参数"。这个过程需要巨大的算力，花几个月时间，用成千上万块 GPU。
- **推理（Inference）**：是学生参加考试的过程。你给他一道题（输入文本），他用学到的知识给出答案（输出文本）。

本书关注的是**推理**。训练是一次性的重活（虽然很贵），推理是持续不断的日常——每当你用 ChatGPT、DeepSeek 回答一个问题，背后就是一次推理。

> **一句话总结**：训练是"教会模型"，推理是"使用模型"。本书讲后者。

---

## 1.2 "生成一个字"到底发生了什么

大模型生成文字的方式出人意料地简单：**每次只预测下一个字（token）**。

假设你输入了"今天天气"，模型要做的事情是：

```
输入: "今天天气"
  ↓ 模型计算
输出: "真"     ← 模型预测下一个字大概率是"真"
  ↓ 把"真"加到输入里
输入: "今天天气真"
  ↓ 模型再算
输出: "好"
  ↓ 重复……
```

就这样一个字一个字地"接龙"，直到模型觉得该停了（遇到结束符），或者达到了你设定的长度上限。

这个"预测下一个字"的过程，技术上叫做 **自回归生成（Autoregressive Generation）**。听起来很高级，本质就是反复做同一件事：看前面的字，猜下一个字。

### 但"猜"不是瞎猜

模型不是随机猜的。它给词表里每个可能的字算一个**概率**。比如输入"今天天气"后：

```
"真"  → 概率 0.45
"不"  → 概率 0.20
"很"  → 概率 0.15
"确"  → 概率 0.08
... 其他十几万个字，概率都很小
```

然后根据某种策略（比如直接选概率最高的，或者按概率随机抽）选出下一个字。这个"给每个字算概率"的输出，叫做 **logits**（一组原始分数），经过 softmax 变成概率。

---

## 1.3 Token：模型眼中的"字"

人类看到的是文字，模型看到的是 **token**。

Token 不完全等于"一个字"或"一个词"。它是一种把文本切分成小块的编码方式。比如：

```
"Hello, world!"  →  [9906, 11, 1917, 0]   ← 4 个 token
"你好世界"        →  [57668, 10236, 10348]   ← 3 个 token（大致）
```

模型内部所有计算都是基于 token 编号（整数）进行的。把文字变成 token 的工具叫**分词器（Tokenizer）**，我们会在第 7 章详细讲。

> **为什么要用 token 而不是直接用字？** 因为不同语言的字符太多（中文有几万字），直接用字符编号会让词表巨大。token 是一种聪明的折中：常用词用一个 token，生僻字拆成多个字节级 token。

---

## 1.4 推理的两个阶段：Prefill 与 Decode

这是理解推理性能最关键的一个概念。

当你给模型一段很长的输入（比如"请帮我总结以下文章：[3000字文章]"），推理分两步走：

```
┌──────────────────────────────────────────────────┐
│  阶段一：Prefill（预填充）                          │
│                                                    │
│  一次性处理整段输入（几百上千个 token）               │
│  → 计算量大，但可以并行处理                          │
│  → 产出：第一个输出 token + 一堆中间结果（KV 缓存）  │
│                                                    │
│  类比：你读完一整篇文章，脑子里记住了内容             │
├──────────────────────────────────────────────────┤
│  阶段二：Decode（解码）                             │
│                                                    │
│  每次只处理 1 个 token（上一步生成的）               │
│  → 计算量小，但必须串行（一个接一个）                 │
│  → 依赖 Prefill 阶段存的 KV 缓存                    │
│                                                    │
│  类比：你根据记忆，一个字一个字地写总结              │
└──────────────────────────────────────────────────┘
```

**为什么这样分？** 因为 Prefill 时输入是一整段，可以并行算（GPU 擅长）；Decode 时每次只有 1 个新 token，必须等上一个算完才能算下一个，天然串行。

这就是为什么你在用 AI 的时候会看到两种现象：
1. **首字延迟**：输入越长，等第一个字出现的时间越久（Prefill 在算）
2. **生成速度**：开始输出后，每个字出现的速度相对稳定（Decode 在跑）

ds4 把这两条路径分得很清楚，我们会在第 16 章对照源码讲透。

---

## 1.5 为什么推理是个难题

你可能会想：不就是矩阵乘法吗，有什么难的？难在以下几点：

### 1. 模型太大

DeepSeek V4 Flash 模型文件有 **86GB**。你的电脑内存（或显存）装得下吗？装不下怎么办？ds4 用了 SSD 流式加载（第 21 章）来解决这个问题。

### 2. 计算量惊人

每一层推理都要做大量矩阵乘法。DeepSeek V4 Flash 有 43 层，每层有注意力计算、前馈网络、MoE 路由等。生成一个 token 就要把 43 层全跑一遍。

### 3. 内存带宽是瓶颈

生成阶段（Decode）每次只处理 1 个 token，计算量其实不大，但要把几十 GB 的权重从内存读到计算单元——**搬运数据比计算本身还慢**。这叫"内存带宽瓶颈"。

### 4. 长上下文很贵

上下文越长，注意力计算需要"看"的历史就越多。KV 缓存（第 15 章）会随上下文线性增长。128K 上下文的 KV 缓存可能比模型本身还大。

### 5. 多用户并发

如果很多人同时用，怎么共享 GPU？怎么排队？ds4-server（第 23 章）解决了这个问题。

> **ds4 的核心价值**：它用了很多工程技巧来应对这些挑战——量化压缩权重（第 18 章）、GPU 内核优化（第 19 章）、Flash Attention（第 20 章）、SSD 流式加载（第 21 章）、推测解码（第 22 章）等。本书会逐一拆解。

---

## 1.6 ds4 是什么

**ds4**（又名 DwarfStar）是一个专门为 DeepSeek V4 系列模型优化的**原生推理引擎**。由 antirez（Redis 的作者 Salvatore Sanfilippo）主导开发。

它的特点：

- **用 C 语言写的**：不是 Python，没有 PyTorch 依赖，直接操作内存和 GPU
- **专精而非通用**：不像 llama.cpp 支持所有模型，ds4 只针对 DeepSeek V4 / GLM 5.2 做深度优化
- **三后端**：Metal（Apple Silicon）、CUDA（NVIDIA）、CPU
- **自包含**：模型加载、分词、推理、HTTP 服务、AI Agent 全在一个代码库里

```
┌─────────────────────────────────────────┐
│              ds4 代码库全景               │
├──────────┬──────────┬────────────────────┤
│  ds4.c   │ 64891 行 │ 核心引擎（加载+算子）│
│ds4_metal │ 39615 行 │ Metal GPU 实现     │
│ds4_cuda  │ 27666 行 │ CUDA GPU 实现      │
│ds4_server│ 17539 行 │ HTTP 服务端        │
│ds4_agent │ 11185 行 │ AI Agent           │
│ds4_dist  │  8426 行 │ 分布式推理         │
│  其他     │ ~27000 行│ 工具/测试/辅助      │
├──────────┴──────────┴────────────────────┤
│           总计约 18 万行 C 代码             │
└─────────────────────────────────────────┘
```

> **为什么选 ds4 学推理？** 因为它代码干净、注释详尽、没有抽象层。你看到的每一行都在做实事，没有框架的黑盒。antirez 的代码风格以"直白"著称，非常适合用来学原理。

---

## 1.7 本书的学习路线

```
第一部分 基础篇        ← 你现在在这里
  建立全局观，跑通第一次推理

第二部分 模型篇
  理解模型文件格式和架构

第三部分 算子篇        ← 核心重点
  逐个吃透每个计算算子

第四部分 推理篇
  把算子串成完整推理流程

第五部分 优化篇        ← 进阶
  量化、GPU 内核、Flash Attention

第六部分 工程篇
  服务化、分布式、性能调优
```

建议按顺序读，因为后面的章节建立在前面概念之上。如果你已经有基础，可以直接跳到感兴趣的章节，每章开头都有"前置知识"提示。

---

## 本章小结

- 推理 = 使用训练好的模型，每次预测下一个 token
- 推理分 Prefill（批量处理输入）和 Decode（逐个生成）两阶段
- 推理的难题在于：模型大、计算多、内存带宽瓶颈、长上下文贵、多用户并发
- ds4 是一个用 C 写的专用推理引擎，代码直白适合学习

## 动手实验

1. 想一想：你在用 ChatGPT 时，"首字延迟"和"打字速度"分别对应推理的哪个阶段？
2. 猜一猜：为什么 Decode 阶段每个 token 的计算量不大，但速度却可能成为瓶颈？（提示：想想数据搬运）
3. 准备好你的开发环境：下一章我们会编译并运行 ds4

## 下一章预告

第 2 章，我们会把 ds4 编译出来，跑通第一次推理，用 `--inspect` 命令窥探模型的内部结构。让你亲手感受一下"推理"到底跑起来是什么样的。
