# 第 21 章 SSD 流式加载与内存管理

## 本章导读

86GB 的模型不是每台机器都有 86GB 内存。ds4 的 SSD 流式加载让小内存机器也能跑大模型--把不用的专家权重放在 SSD 上，用到时才加载。这章我们讲这个"虚拟内存增强"机制。

读完本章你会理解：
- 为什么 mmap 不够用
- SSD 流式加载的原理
- 专家预取如何隐藏加载延迟
- 内存预算的计算

**前置知识**：第 6、13 章

---

## 21.1 为什么 mmap 不够

第 6 章讲过，mmap 可以按需加载文件页面。那为什么还需要 SSD 流式？

```
mmap 的问题:
  1. 操作系统按 4KB 页面管理, 对 86GB 文件效率不高
  2. 页面置换策略由 OS 控制, 不可预测
  3. 页面被淘汰后, 重新加载时产生不可控的延迟
  4. 对于 MoE 模型, 256 个专家的访问模式不均匀
     -> OS 不知道哪些专家即将被用到

SSD 流式的优势:
  1. 应用层控制加载策略 (按专家粒度)
  2. 可以预取: 在算当前专家时, 预加载下几个专家
  3. LRU 缓存: 保留最近使用的专家在内存
  4. 精确控制内存使用量
```

> **关键区别**：mmap 是"被动等待"（页面缺失时才加载），SSD 流式是"主动管理"（预取 + 缓存控制）。

---

## 21.2 SSD 流式的架构

```
┌─────────────────────────────────────────────────┐
│  模型文件 (86GB, 在 SSD 上)                       │
│  [Embedding] [Layer0 权重] [Layer1 权重] ...     │
│  [专家0] [专家1] ... [专家255] [Layer1 专家] ...  │
└─────────────────────────────────────────────────┘
                        │
                        │ 按需加载 + 预取
                        ▼
┌─────────────────────────────────────────────────┐
│  内存缓存 (如 16GB)                               │
│  ┌─────────────────────────────────────────┐    │
│  │ 常驻部分 (Embedding + 注意力权重 + ...)   │    │
│  │ ≈ 13GB (非路由专家权重)                   │    │
│  ├─────────────────────────────────────────┤    │
│  │ 专家缓存 (LRU, ≈ 3GB)                    │    │
│  │ [专家42] [专家128] [专家7] ... (最近用的) │    │
│  └─────────────────────────────────────────┘    │
└─────────────────────────────────────────────────┘
```

### 内存预算分配

`ds4_ssd.h` 中的 `ds4_ssd_cache_plan` 描述缓存策略：

```c
typedef struct {
    uint64_t model_target_bytes;    // 目标内存使用量
    uint64_t cache_bytes;           // 专家缓存字节数
    uint64_t effective_cache_bytes; // 有效缓存字节数
    uint32_t cache_experts;         // 可缓存的专家数量
} ds4_ssd_cache_plan;
```

`ds4_ssd_auto_cache_plan()` 自动计算最优缓存策略：
- 非路由权重（Embedding、注意力等）常驻内存
- 剩余空间用于专家缓存
- 根据 `per_expert_bytes` 计算能缓存多少专家

---

## 21.3 专家预取

专家预取是 SSD 流式的核心优化：**在计算当前专家时，异步加载下一批要用的专家**。

```
没有预取 (串行):
  加载专家A (50ms) -> 计算专家A (10ms) -> 加载专家B (50ms) -> 计算专家B (10ms)
  总时间: 120ms, GPU 闲置 100ms

有预取 (流水线):
  加载专家A (50ms) ────────┐
                            ├─ 计算专家A (10ms)
  预取专家B (50ms) ────────┐  │
                            ├─ 计算专家B (10ms)
  预取专家C (50ms) ────────┐  │
                            └─ ...
  总时间: 50ms + 10ms × N (加载和计算重叠)
```

### 隐藏延迟

```
专家加载: ~50ms (从 SSD 读取)
专家计算: ~10ms (GPU 上算)

预取让加载和计算重叠:
  -> 实际感觉只有 10ms/专家 (加载被隐藏)
  -> 接近全内存的速度!
```

> **共享专家的利用**：第 13 章讲过，每层有一个共享专家始终激活。在计算共享专家的同时，可以预取路由专家。共享专家的计算时间"免费"提供了预取窗口。

---

## 21.4 路由专家的缓存管理

### LRU 缓存

ds4 用 LRU（最近最少使用）策略管理专家缓存：

```
缓存容量: 100 个专家 (举例)

Token 1: 路由选择 [专家42, 7, 128, 201, 55, 99]
  -> 全部从 SSD 加载到缓存
  -> 缓存: [42, 7, 128, 201, 55, 99]

Token 2: 路由选择 [专家42, 7, 33, 88, 201, 12]
  -> 42, 7, 201 已在缓存 (命中!)
  -> 33, 88, 12 从 SSD 加载
  -> 缓存更新 LRU 顺序

Token 3: 路由选择新专家, 缓存满了
  -> 淘汰最久未用的专家
  -> 加载新专家
```

### 缓存命中率

```
256 个专家, 每次选 6 个, 缓存 100 个:
  理论命中率 ≈ 100/256 ≈ 39%
  
实际命中率更高, 因为:
  - 路由有偏好 (某些专家更常用)
  - 连续 token 倾向选相同专家
  - imatrix 训练让路由更集中
```

---

## 21.5 使用 SSD 流式

### 命令行参数

```bash
./ds4 --ssd-streaming --ssd-cache 16G -p "你好"
```

- `--ssd-streaming`：启用 SSD 流式
- `--ssd-cache 16G`：专家缓存 16GB

### 相关参数

在 `ds4_engine_options`（`ds4.h:108-120`）中：

```c
typedef struct {
    // ...
    bool ssd_streaming;              // 启用 SSD 流式
    bool ssd_streaming_cold;         // 冷启动 (不预加载)
    uint32_t ssd_streaming_cache_experts;  // 缓存专家数
    uint64_t ssd_streaming_cache_bytes;    // 缓存字节数
    uint32_t ssd_streaming_full_layers;    // 完全常驻的层数
    uint32_t ssd_streaming_preload_experts;// 预加载专家数
    // ...
} ds4_engine_options;
```

### `ssd_streaming_full_layers`

前 N 层完全常驻内存，不流式：

```c
ssd_streaming_full_layers = 3  // 前 3 层完全在内存
```

因为前 3 层用 hash 路由（第 13 章），每个 token 都可能用不同的专家，流式效率低。让它们常驻内存更合理。

---

## 21.6 性能影响

```
全内存 (96GB Mac):
  Decode: ~15 ms/token (~65 tok/s)
  专家加载: 0ms (全在内存)

SSD 流式 (16GB 缓存):
  Decode: ~25 ms/token (~40 tok/s)
  专家加载: ~10ms (部分命中, 部分从 SSD 加载)
  -> 比全内存慢约 1.7 倍, 但能在小内存机器上跑!

SSD 流式 (冷启动, 无缓存):
  首个 token: ~500ms (加载专家)
  后续 token: ~25ms (缓存逐渐填满)
```

### NAS 存储的问题

如果模型在网络存储（NAS）上，延迟会更高：

```
本地 SSD: 读取延迟 ~0.1ms
NAS: 读取延迟 ~5ms (50 倍)

NAS 上 SSD 流式:
  专家加载: ~250ms (500 倍于本地)
  Decode: ~270ms/token (~3.7 tok/s)
  -> 慢但可用, 远好于"无法运行"
```

> **实际测试**（记忆上下文中的观察）：在 NAS 环境下，GPU 利用率仅 3.6%，瓶颈在 NAS IO 而非计算。这说明存储 I/O 是 SSD 流式的关键瓶颈。

---

## 21.7 KV 检查点：另一种"内存管理"

除了权重流式，ds4 还支持 **KV 缓存检查点**（第 15 章）。这是另一种内存管理策略：

```
长对话场景:
  对话 1: prefill 10K token -> KV 缓存 500MB
  对话 2: 继续对话 -> 复用 KV 缓存

如果内存不够存所有 session 的 KV:
  -> 把不活跃 session 的 KV 保存到磁盘
  -> 需要时从磁盘加载
  -> ds4_session_save_payload() / ds4_session_load_payload()
```

这样可以在有限内存下服务多个用户的对话历史。

---

## 本章小结

- mmap 的页面管理不够精细，SSD 流式提供应用层控制
- SSD 流式按专家粒度管理内存：常驻非路由权重 + LRU 缓存路由专家
- 专家预取在计算共享专家时异步加载路由专家，隐藏 SSD 延迟
- `ssd_streaming_full_layers` 让前几层完全常驻（hash 路由不适合流式）
- 16GB 缓存可以达到全内存 60% 的速度
- NAS 存储会显著增加延迟（I/O 成为主要瓶颈）
- KV 检查点是另一种内存管理：保存/加载对话的 KV 缓存

## 动手实验

1. 在 `ds4_ssd.h` 查看接口定义，理解缓存计划的参数
2. 如果你有 SSD，尝试 `./ds4 --ssd-streaming --ssd-cache 8G -p "test"`，对比速度差异
3. 思考：为什么前 3 层（hash 路由）不适合流式而后面可以？

## 下一章预告

第 22 章，推测解码（Speculative Decoding）--GPU 在 decode 时算力闲置，不如让它"猜"几个 token，猜对了就白赚。
